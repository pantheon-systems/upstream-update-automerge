---
version: 2
jobs:
  test-automerge:
    docker:
      - image: python:2.7.16-stretch
    working_directory: /automerge
    steps:
      - checkout
      - run: python test.py

workflows:
  version: 2
  automerge:
    jobs:
      - test-automerge
